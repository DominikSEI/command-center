upstream backend {
    server backend:8000;
}

upstream frontend {
    server frontend:80;
}

# Rate limiting für Login
limit_req_zone $binary_remote_addr zone=login:10m rate=5r/m;

server {
    listen 80;
    server_name _;

    # Redirect HTTP → HTTPS (in production)
    # return 301 https://$host$request_uri;

    # Agent SSE streaming — must come before generic /api/ block
    location /api/agent/ {
        proxy_pass http://backend/agent/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE requires buffering off and long timeouts
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        chunked_transfer_encoding on;
    }

    # API
    location /api/ {
        proxy_pass http://backend/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Login rate limiting
    location /api/auth/login {
        limit_req zone=login burst=3 nodelay;
        proxy_pass http://backend/auth/login;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Frontend
    location / {
        proxy_pass http://frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

# HTTPS (Let's Encrypt — uncomment and fill in production)
# server {
#     listen 443 ssl;
#     server_name dashboard.yourdomain.de;
#
#     ssl_certificate /etc/letsencrypt/live/dashboard.yourdomain.de/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dashboard.yourdomain.de/privkey.pem;
#
#     location /api/ {
#         proxy_pass http://backend/;
#         ...
#     }
#
#     location / {
#         proxy_pass http://frontend;
#         ...
#     }
# }
